---

- name: Install tmux
  package:
    name: tmux
    state: present
  become: true

- name: Copy config
  template:
    src: templates/tmux.conf.j2
    dest: /home/{{username}}/.tmux.conf
    owner: "{{username}}"
    group: "{{username}}"
    mode: 0640
  become: true
